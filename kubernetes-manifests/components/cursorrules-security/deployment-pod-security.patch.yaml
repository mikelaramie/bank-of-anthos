# .cursorrules §1.1 — pod-level PSS on all Deployments (CIS 4.6.2)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: _patch
spec:
  template:
    spec:
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
